# V2-INNOVATION-011 结果

状态：`rescue2_beta20_planned`。

RUN-001 H=`76.407873`，相对父模型提高`0.401026`，但beta=`4.985551/5`接近上限。

RUN-002 H=`76.856269`，相对父模型提高`0.849421`，U/S/ZS均提高；beta=`9.943932/10`仍接近上限，说明类名残差有效，但当前允许范围仍不足。

RESCUE-2只把同一个已验证残差的beta上限扩大到20，不改变模块、loss、数据或评估语义。一次不写结果文件的只读整数网格诊断显示beta约16可能达到目标；该观察不是正式RUN结果，必须由RUN-003按陈式训练与official-test选模流程重新产生并保存权重后才能采信。
