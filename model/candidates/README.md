# 候选代码

这里保存尚未接纳或已经失败的研究代码，按产生版本分区。候选状态和实验结论以 `research/ideas/` 与 `experiments/vX/EXPERIMENT_QUEUE.csv` 为准。

- `v2/modules/`：V2 候选模块。
- `v2/trainers/`：对应训练入口与统一训练器。
- `v2/early_tries/`：早期快速尝试。
- `v3/`：V3 探索代码。
- `v4/`：V4 当前候选，每个 Idea 独立目录。

候选可以调用正式框架；正式框架不能反向依赖候选。
